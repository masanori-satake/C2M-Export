このファイルは、このプロジェクトに関わるエージェント（AI）向けの指示書です。

コミュニケーション

*   ユーザーとのすべてのコミュニケーション、プルリクエスト（PR）のコメントなどは、すべて日本語で行ってください。

プロジェクト概要

*   プロジェクト名: C2M-Export
*   目的: Confluence Data Center のページツリーを Markdown 形式にエクスポートし、AIナレッジベース向けのインプットを作成する。
*   参照プロジェクト: J2M-Export および G2M-Export との共通性（引数の仕様や振る舞い）を維持し、利用者の学習コストを最小限に抑えること。
*   サポート対象: 現状は Confluence Data Center 9.2 以降を対象とするが、将来的な Cloud 版への対応を見据えた拡張性の高い設計・実装を行うこと。

開発ルールと技術仕様

1. 依存関係と環境構築

*   実行に必要な依存関係は `requirements.txt` に記載されています。
*   開発・テスト用の依存関係（pytest など）は必要に応じて手動でインストールするか、将来的に `requirements-dev.txt` に集約してください。
*   テストの実行は `PYTHONPATH=. python -m pytest` を使用してください。

2. 設定管理

*   設定は CLI 引数、環境変数、YAML 設定ファイルの順で優先されます（CLI > YAML > デフォルト）。
*   デフォルトの設定パスは `c2m_config.yaml` です。
*   Confluence のベース URL を読み込む際は、一貫性を保つため末尾のスラッシュを削除（rstrip('/')）して正規化してください。

3. エクスポートロジック

*   ページツリーの取得には深さ優先探索（DFS）を使用し、階層構造を維持したまま 1 つの Markdown ファイル（またはルートページ毎のファイル）に統合します。
*   サイズ制限機能: `max_mb`（最大許容サイズ）と `stop_threshold_mb`（処理停止閾値）に基づき、肥大化を防止します。閾値を超えそうな場合は、新しいページの取得を停止し、それまでの内容を出力します。
*   複数ルートページ対応: `root_page_ids` はリストとして受け取り、順次処理を行います。

4. Confluence API と クライアント

*   認証には Bearer トークンを使用します（`Authorization: Bearer <token>`）。
*   HTTP/HTTPS プロキシ経由のアクセスをサポートしてください。
*   ページ内容の取得には `/rest/api/content/{id}` を使用し、`expand=body.storage,space` パラメータで Storage Format (XHTML) を取得します。
*   大規模な子ページ一覧の取得には、`limit` と `start` によるページング処理を実装してください。
*   `ConfluenceClient` には、サーバーエラー（5xx）に対する指数バックオフを伴う自動再試行ロジックを実装してください。

5. データ処理と変換

*   HTML から Markdown への変換には `beautifulsoup4` を使用してください。
*   マクロ変換: `code`, `plantuml`, `status`, `jira`, `include` などの主要なマクロを適切に Markdown 形式に変換し、ナビゲーション系（`toc`, `children` 等）は AI ナレッジ向けに不要なため空文字として処理してください。
*   メタデータの付与: エクスポートされる各ページの先頭に、Page ID, Space Key, URL などのメタ情報を付与してください。
*   ファイルの書き込みには UTF-8 エンコーディングを明示的に指定してください。

6. コード品質

*   `.pre-commit-config.yaml` で設定された pre-commit フックを使用し、コード品質を維持してください。

7. コメントとドキュメント

*   コード内のコメントや docstring はすべて日本語で記載してください。
*   docstring は PEP 257 に準拠し、実装の意図や背景を重点的に記載してください。

8. ログ出力とエラーメッセージ

*   ログ出力はすべて日本語で行ってください。
*   特にエラーメッセージは、以下の構成を強く推奨します：
    *   現象: 何が起きたか
    *   原因: なぜ起きたか（詳細な例外情報を含める）
    *   対処方法: ユーザーが次に何をすべきか
    *   例: 「設定ファイルの読み込みに失敗しました（現象）。詳細: {e}（原因）。c2m_config.yaml のパスと権限を確認してください（対処方法）」

9. Web UI (Streamlit)

*   Streamlit を使用したブラウザベースの UI を提供します。
*   Web UI に関する開発は、特定の UI 仕様に固執するのではなく、バックエンドのロジックを安全かつ直感的に呼び出せることに重点を置いてください。
*   複数ユーザーの同時実行を防ぐための排他制御（ロック機構）を適切に維持してください。

10. README の保守

*   新機能の追加や仕様変更、エラーメッセージの追加などを行った際は、必ず `README.md` および `README_DEV.md` を更新してください。
